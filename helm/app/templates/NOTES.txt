Application demo-java-app deployed successfully!

Access the application:
  export NODE_IP=$(kubectl get nodes -o jsonpath="{.items[0].status.addresses[0].address}")
  echo "URL: http://$NODE_IP:{{ .Values.service.nodePort }}"

Useful commands:
  kubectl get pods -l app={{ .Values.appName }}
  kubectl get svc {{ .Values.appName }}-service
  kubectl get hpa {{ .Values.appName }}-hpa
